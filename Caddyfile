# HTTPS with automatic Let's Encrypt certificate

openpanel.ryqo.ai {
reverse_proxy openpanel:6515
}

# Also serve on HTTP (redirects to HTTPS)

http://openpanel.ryqo.ai {
redir https://openpanel.ryqo.ai{uri}
}
